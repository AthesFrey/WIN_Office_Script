# v5.6fix3 检查结果

检查日期：2026-09-17，Linux 环境，目标为 Windows 桌面版 Excel 2021/2024。当前无法运行桌面 Excel；文件和解析检查不等同于 Excel 编译或实际操作通过。

## 已执行

- `uv sync --locked` 通过。工程版本 `5.6.3`，全部依赖锁定条目和构建模板 `WBS.xlsm` 与 fix2 相同。
- 构建得到 `dist/WBS-Gantt-ProgressChart-v5.6fix3.xlsm`。
- `uv run pytest -q`：**24 项通过**，19 条警告来自既有 oletools/pyparsing 弃用提示。保留排期、进度、字体、按钮绑定、包关系、VBA 存储与压缩检查。
- 工具栏检查要求两张表全部按钮使用固定工作表锚点，不含单元格标记，且形状与锚点的坐标、尺寸完全一致。Milestones 的 11 个按钮继续满足原定位置、3 磅初始上间距、25 磅高度、横向间隔及预留行范围。
- 对实际交付工作簿运行同一检查：fix2 被固定锚点检查拒绝，fix3 通过；可检出原定位问题，不依赖事件或宏复位。
- 修改的 `Sheet4.cls` 和 `ProgressChart.bas` 通过 ANTLR VBA 7.1 语法解析，0 错误。其余三份源码与 fix2 字节相同。五份嵌入源码与当前源码经 CRLF 标准化后完全一致，物理行均小于 1,023 字节。ANTLR 不执行 Excel 编译或 COM 类型检查。
- 两张表、两份 DrawingML 和样式通过 ECMA-376 schema 校验；新版 Office 扩展属性按检查器说明排除。
- `excel_smoke.ps1`、`excel_milestones.ps1`、`excel_regressions.ps1` 三份脚本通过 PowerShell 7.4.6 语法解析，**未执行 Excel 场景**。
- 与 fix2 的工作簿对比仅三处包内容变化：VBA、Milestones 图形、WBS 版本标题。其他包内容字节一致；WBS 工作表替换版本标签后也完全一致。数据、样式、验证、WBS 工具栏和原图形几何保持一致。
- 复核删除了单元格锚点换算、`move_row` 分支、无用导入、布局保护变量、重复坐标表、`ResetToolbarLayout` 和两处调用。源码及嵌入代码中无旧复位引用；测试改为直接验证固定定位。全部用户按钮宏入口和绑定保留。
- 按钮预览从保存的形状实际坐标绘制，并检查横向间隔及与设置、状态、表头区域的间隔；单元格文字为简化展示，不是 Excel 截图。样例图形预览同样从 DrawingML 绘制。
- 交付 ZIP 的 CRC 和全部 34 个载荷文件 SHA-256 通过；解压其中的源码工程到临时目录重新构建，XLSM 内每个部件均与交付产物一致。单独提供的 XLSM 与 ZIP 内工作簿完全一致，包内含本次修复说明、两张预览和可重建源码。


## 已准备、未执行的 Excel 实测

在可信位置运行 `tests/excel_smoke.ps1`，自动载入另外两份脚本。只读打开交付工作簿，不覆盖保存原件；布局检查保存、重开并删除临时副本，不更改宏安全设置。

更新的布局场景检查 `Placement = 3`（不随单元格移动或调整大小）及每个按钮的 Left/Top/Width/Height，覆盖首次打开、80%/100%/125% 缩放、增减列宽行高、单元格向右下方剪切移动再移回。每次改变后、任何激活或按钮宏运行前立即核对位置，同时覆盖事件关闭和开启状态；另覆盖切页、Generate 按钮、全刷新以及修改行列后的保存重开。

保留异常输入、排期、混合格式粘贴、两页三步历史、快捷键、五级删除、保护回放、来源结构变化及 3,000 任务检查。实际鼠标拖拽、Excel 编译/修复提示、另存为、剪贴板导出及其他交互仍待桌面实测；没有声称上述运行测试或性能测试已通过。
