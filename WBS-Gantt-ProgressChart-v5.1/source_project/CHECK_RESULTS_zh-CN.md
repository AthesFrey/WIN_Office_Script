# v5.1 检查结果

检查环境：Linux，2026-09-16。当前没有桌面版 Microsoft Excel，以下结果不等于 Excel 实机运行通过，也没有实测提速倍数。

## 已执行并通过

- 按 `uv.lock` 同步依赖；依赖及版本均未变，仅项目版本从 5.0.0 更新为 5.1.0。
- 构建 `WBS-Gantt-ProgressChart-v5.1.xlsm`。
- **18 项文件回归测试通过**，覆盖工作簿关系、样式、内嵌 VBA 与源码一致性、按钮入口、日期和父级汇总、图形几何及压缩容器；19 条提示为已有依赖的弃用警告。
- `Model_1.bas`、`ProgressChart.bas`、`Sheet1.cls` 均通过 VBA 7.1 语法检查，**0 个语法错误**。这不执行 Excel 或 COM 调用。
- 两张工作表及两份 DrawingML 通过 OOXML schema 检查；工作簿和 VBA 容器可解析，无失效内部关系或旧编译缓存。
- 示例从 59 行减为 14 行；保留行 B:Y 的所有值与 v5 相同。保留示例的时间轴为 2018/8/1～2018/9/3，父任务工期和叶子加权进度一致。原始构建模板未改动。
- 仍支持 1～5 级；新增 Promote / Demote 按钮已绑定实际宏入口。
- 图形预览已重新生成并查看。三角中心对齐红色横杆的可见上边缘；最近日期框底部到三角顶端为 2 磅。保存示例和生成宏采用一致的尺寸设置。预览不是 Excel 截图。
- Milestones 页顶部说明从 648 字符减为 186 字符，同时删除每个示例的重复注释。
- 删除不再使用的 `code.vbs`、旧递归排期/逐行前置查找实现、单行层级调整实现及未使用状态变量。修正“更新全部”在排期失败后继续生成里程碑的问题。
- 打包时检查 ZIP 完整性、包内工作簿一致性及全部文件 SHA-256 校验和。

## 已准备、未执行的 Excel 回归

随包 `tests/excel_smoke.ps1` 增加多选下拉、连续/不连续/整行选择、父子重叠选择、前置引用随编号更新、非法批量级别、循环依赖、手动计算模式以及 3,000 条前置任务链的验证与计时。旧版日期、插入任务、甘特图清理、导入联动、密集里程碑等场景保留。

脚本只修改内存中以只读方式打开的工作簿，关闭不保存，不更改宏安全设置。仍需桌面 Excel 核验实际按钮、事件、宏编译、无修复提示、剪贴板导出和保存后重开；实际运行时间应以本机 Excel 测量为准。
